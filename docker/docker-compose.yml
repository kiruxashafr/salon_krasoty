services:
  server:
    build:
      context: ..
      dockerfile: docker/Dockerfile.server
    ports:
      - "3011:3011"
    volumes:
      - ../database.db:/app/database.db
      - ../photo:/app/photo
      - ../sh:/app/sh
      - ../back:/app/back
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "--max-time", "10", "-f", "http://localhost:3011"]
      interval: 30s
      timeout: 10s
      retries: 1

  bot:
    build:
      context: ../bot
      dockerfile: ../docker/Dockerfile.bot
    volumes:
      - ../bot:/app
    environment:
      - API_BASE_URL=http://server:3011
    depends_on:
      server:
        condition: service_healthy
    restart: unless-stopped